syntax = "proto3";

message MoveIntent {
    // TODO: Optimize into byte enum flag
    bool forward = 1;
    bool backward = 2;
    bool left = 3;
    bool right = 4;
}

message Velocity {
    float velocity_x = 1;
    float velocity_y = 2;
    float velocity_z = 3;
}